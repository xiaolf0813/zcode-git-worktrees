{
  "name": "zcode-git-worktrees",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "Every thread automatically gets its own isolated git worktree — parallel work with zero setup. Snapshot-safe removal, background agent tasks, PR checkouts, and retention cleanup included.",
  "author": "Alfredo Gallardo (https://github.com/agallardol)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/agallardol/zcode-git-worktrees"
  },
  "scripts": {
    "test": "node --test --test-concurrency=2 tests/unit/*.test.mjs tests/integration/*.test.mjs tests/adversarial/*.test.mjs",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:integration": "node --test tests/integration/*.test.mjs",
    "test:adversarial": "node --test tests/adversarial/*.test.mjs"
  }
}
